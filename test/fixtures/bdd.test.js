describe('bdd', () => {
  before(() => {})
  after(() => {})
  beforeEach(() => {})
  afterEach(() => {})

  it('works', () => {})
  specify('alias', () => {})

  context('nested', () => {
    it('also works', () => {})
  })
})
